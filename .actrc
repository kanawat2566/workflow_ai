# act — Run GitHub Actions locally (https://github.com/nektos/act)
# ติดตั้ง: choco install act-cli  หรือ  winget install nektos.act

# ใช้ Docker image ขนาดเล็ก (medium = ~500MB, large = ~17GB)
-P ubuntu-latest=catthehacker/ubuntu:act-22.04

# Map self-hosted runner
-P self-hosted=catthehacker/ubuntu:act-22.04

# ใช้ .env สำหรับ secrets
--env-file .env
